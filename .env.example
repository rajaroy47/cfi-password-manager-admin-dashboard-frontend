# Base URL of the backend API. Change this per office deployment without
# rebuilding the app if you serve it via a runtime-configurable value
# (see README "Configuring the API URL without rebuilding").
VITE_API_BASE_URL=http://192.168.1.100:4000
